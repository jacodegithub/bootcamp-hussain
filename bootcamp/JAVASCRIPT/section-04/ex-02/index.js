// Create a class called Library with a property books that is an array to store books. Implement a constructor that initializes an empty array.

class Library {
    constructor() {
        this.books = [];
    }
}

const lib = new Library();

console.log(lib);